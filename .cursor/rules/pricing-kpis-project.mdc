---
description: Core context for Jeeny Daily Pricing KPIs alert project
alwaysApply: true
---

# Daily Pricing KPIs (Jeeny)

- Preserve the locked fare-integrity `fare_diff` headline; discount analysis is
  an additive post-discount passenger view (`net_fare_diff`, `d_discount`).
- Active SQL: `sql/priceshocks_daily_digest_v2.sql`.
- BI full rebuild: `sql/bi_priceshocks_daily.sql` (delete+insert into
  `JEENY_PROD.RIDE.PRICESHOCKS`). Schedule: **11:00 AM PKT**.
- Active instructions: `automations/DAILY_SLACK_INSTRUCTIONS_V2.md`.
- Comparisons: **DoD**, **WoW**, **MoM = yesterday vs 28 days before**.
- SA surcharge VAT ×1.15; JO ×1.0. issue_types: matched/rounding/increase_non_issue/increase_pricing/decrease_pricing.
- Keep the 30-day spillover lookback. Never sum SAR and JOD.
- Memory: `memory/PROJECT_CONTEXT.md`. Discount spec:
  `docs/price-shock-discounts-implementation-spec.md`. BI handoff:
  `docs/price-shock-discounts-bi-handoff.md`.
- Never invent formulas; update docs when SME facts change.
